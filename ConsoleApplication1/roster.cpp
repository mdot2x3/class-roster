#include <iostream>
#include <string>
#include "roster.h"

using namespace std;

void parse(string row) {
	string studentValues = const string studentData[string row];


	add();
}

void add(string studentID, string firstName, string lastName, string emailAddress, int age, int daysInCourse1, int daysInCourse2, int daysInCourse3, DegreeProgram degreeProgram) {
	for (int i = 0; i < 5; i++) { //maybe all of this for stuff goes in main.cpp and here we just keep add as its most basic function which is then called inside parse which is called in main??
		if (classRosterArray[i] == null) {
			classRosterArray[i] ---OR--- Student* S = new Student(string studentID, string firstName, string lastName, string emailAddress, int age, int daysInCourse1, int daysInCourse2, int daysInCourse3, DegreeProgram degreeProgram);
		}
	}	
}

